meta {
  name: Update settings
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/admin/settings
  body: json
  auth: inherit
}

body:json {
  {
    "scrape": {
      "enabled": true,
      "schedule": "0 0 8-15 ? * * *"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
